<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.vicente.appgenesis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/luz_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/luz_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.ResultsActivity"></activity>
        <activity android:name=".view.InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>